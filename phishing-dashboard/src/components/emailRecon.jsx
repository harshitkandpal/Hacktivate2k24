export const emailRecon = async (domain) => {
    // Mock data for email reconnaissance
    const emails = [
      'user1@' + domain,
      'user2@' + domain,
      'user3@' + domain,
      'user4@' + domain,
    ];
  
    return emails;
  };
  